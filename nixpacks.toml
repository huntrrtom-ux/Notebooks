[phases.setup]
nixPkgs = ["...", "python3", "yt-dlp", "ffmpeg"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["cd client && npm install && npx vite build"]

[start]
cmd = "node server/index.js"
